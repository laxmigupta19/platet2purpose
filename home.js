function redirectTo(role) {
    if (role === "donate") {
      window.location.href = "login.html"; // Change this if needed
    } else if (role === "recipient") {
      window.location.href = "login1.html"; // Change this if needed
    }
  }
  
  console.log("Page ready - Share Food Landing Page");
  